let [css, userinfo, refresh, flip, history] = [
  require('./css'),
  require('./userinfo'),
  // require('./refresh'),
  // require('./flip'),
  // require('./history')
]
const mocks = [
    css,
    userinfo,
    // refresh,
    // flip,
    // history
]

module.exports = mocks
